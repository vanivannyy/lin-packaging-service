# Salin file ini menjadi `.env` lalu isi dengan nilai asli (jangan pernah commit `.env`).

# Connection string PostgreSQL. Untuk hosting cloud (Neon/Supabase/Railway) gunakan
# connection string versi "pooled" jika tersedia agar cocok dengan serverless (Vercel).
DATABASE_URL="postgresql://user:password@localhost:5432/lin_packaging?schema=public"

# Secret untuk menandatangani session JWT. WAJIB diganti dengan string acak & panjang di production.
# Generate contoh: openssl rand -base64 32
SESSION_SECRET="change-this-secret-in-production"
